# QA notes — 2026-09-02

The live Menu route returned 10,887 product rows from `/api/trpc/products.list`. The first rows have unique generated-looking SKUs such as `XLSX-00020`, while nameHy/nameRu/nameEn are often identical Armenian strings and price is 0. A SKU-only deduplication would therefore preserve all imported store-level rows. The source image shows repeated records by GroupID/store, so the catalog needs identity deduplication using a stable barcode when available, otherwise a normalized product name + package/unit/variant key. The customer-facing Menu should not expose the raw 10,887 rows as one continuous list.

The browser screenshot also confirmed the responsive header renders separate language, favorites, and cart controls without overlap at the tested viewport. Pagination changes were compiled successfully, but the current browser process may still be serving an older hot state and must be refreshed/restarted before final verification.

A second runtime analysis confirmed 10,887 rows but only a much smaller unique-name set; the top duplicate is `կեչու տերեւներ 20գ` with 2 rows, while many 911 products appear as distinct names. Several imported rows have `nameRu` and `nameEn` null, so product translation completeness requires enriching those source fields rather than relying on the current fallback. A stable product identity should use normalized name + package/unit, with a barcode if the original source has one; generated `XLSX-xxxxx` SKUs are not suitable for deduplication.

After restarting the local server and reopening `/menu`, the page still showed `10887 արդյունք`. This indicates the browser was likely connected to a different already-running server process/port or the restart command did not replace the active process. The source build is successful, but the live runtime must be verified against the exact process serving port 3000 before delivery. The Menu viewport itself shows the language, favorites, and cart controls separated and the product card action row includes cart plus favorite.

After the local fallback deduplication patch and server restart, the live `products.list` response contains 10,886 rows and 10,886 unique normalized name+unit keys (down from 10,887 raw rows). The deduplication is active in the live process and the first/last records are returned correctly. This confirms there was only one exact normalized-name duplicate in the current imported dataset; the image's repeated store/group rows mostly represent distinct imported names or variants and still require source-level review if more semantic merging is desired.

Final browser QA: checkout now shows `Nova Pharm · Մուշ`, the Mush address, `08:30–22:30`, and the new note `Պատվերը կուղարկվի Կոմիսարների 76ա դեղատուն՝ պատրաստման համար։`; the old phone-confirmation sentence is gone. Menu now shows `10886 արդյունք`, product units render as `տուփ`, and the responsive header/card action controls remain separated. Compact pagination is compiled and should be used for the large catalog.
